# 完整功能测试计划

**创建日期**: 2025-01-12  
**测试工具**: Chrome DevTools MCP (外部Chrome浏览器) + Filesystem MCP (CSV文件管理)  
**测试环境**: Windows 10, Next.js 14 前端应用, FastAPI 后端  
**测试目标**: 完整功能回归测试，确保所有功能正常工作  
**测试结果保存**: 使用 **Filesystem MCP 工具** 直接编辑和保存 `test_results.csv`（不使用Python脚本，不生成Markdown文件）

---

## 📋 测试原则

### 测试顺序原则
1. **先基础后高级** - 先测试基础功能（登录、注册），再测试高级功能
2. **先独立后依赖** - 先测试独立功能，再测试依赖其他功能的功能
3. **先核心后辅助** - 先测试核心业务功能，再测试辅助功能
4. **先修复后验证** - 先修复已知问题，再验证修复效果

### 测试依赖关系
```
注册/登录 → Dashboard → 项目管理 → 文件管理
                ↓
            Chat功能
                ↓
            团队管理 → 项目用户添加
```

---

## 🎯 测试阶段划分

### Phase 1: 基础功能测试（必须先通过）
**目标**: 确保用户能够正常注册和登录系统

### Phase 2: Dashboard功能测试
**目标**: 验证Dashboard页面加载和基本功能

### Phase 3: 项目管理功能测试
**目标**: 验证项目创建、查看、编辑等功能

### Phase 4: 文件管理功能测试
**目标**: 验证文件上传、查看、管理等功能（验证上周修复的性能优化）

### Phase 5: Chat功能测试（重点）
**目标**: 验证AI问答功能是否正常工作（解决无法使用问题）

### Phase 6: 团队管理功能测试（重点）
**目标**: 验证项目用户添加功能是否正常工作（解决无法添加问题）

### Phase 7: 性能验证测试
**目标**: 验证上周修复的性能优化是否生效

---

## 🔧 测试工具使用说明

### Chrome DevTools MCP工具
- **浏览器**: **必须使用外部Chrome浏览器**（不是内置浏览器）
  - ⚠️ **重要**: 测试必须在外部Chrome浏览器中进行
  - ⚠️ **禁止**: 不能使用Cursor内置浏览器
  - **操作方式**: 
    - 使用 `browser_navigate` 导航到页面（在外部Chrome浏览器中打开）
    - 使用 `browser_snapshot` 获取页面快照
    - 使用 `browser_click` 点击元素
    - 使用 `browser_type` 输入文本
    - 使用 `browser_console_messages` 查看控制台消息
    - 使用 `browser_network_requests` 查看网络请求

### 测试结果保存
- **保存位置**: **桌面 `C:\Users\<用户名>\Desktop\test_results.csv`**
  - ⚠️ **重要**: Filesystem MCP工具只能访问Desktop和Documents目录
  - ⚠️ **不能**: 不能保存在项目根目录（不在允许的目录范围内）
- **文件格式**: CSV表格（UTF-8编码，可用Excel打开）
- **保存方式**: 使用 **Filesystem MCP 的 `edit_file` 工具** 直接编辑CSV文件
  - 工具路径: `C:\Users\<用户名>\Desktop\test_results.csv`
  - 使用 `mcp_filesystem_edit_file` 工具更新测试结果
- **记录内容**: 测试阶段、测试项、状态、耗时、错误信息、备注
- **不使用**: Python脚本、Markdown文件、代码更新方式、项目根目录

---

## 📝 详细测试步骤

## Phase 1: 基础功能测试

### 1.1 环境准备
**测试步骤**:
1. 使用终端命令检查前端服务是否运行（http://localhost:3000）
2. 使用终端命令检查后端服务是否运行（http://localhost:8001）
3. 使用Chrome DevTools MCP导航到首页，检查页面是否正常加载

**预期结果**:
- ✅ 前端服务正常运行
- ✅ 后端服务正常运行
- ✅ 页面正常加载

**如果失败**: 停止测试，修复服务问题

**记录方式**: 在测试结果表格中记录状态和备注

---

### 1.2 首页测试
**测试步骤**:
1. ⚠️ **确保在外部Chrome浏览器中打开**（不是内置浏览器）
2. 使用 `browser_navigate` 导航到首页 (`http://localhost:3000/`)
   - 这会在外部Chrome浏览器中打开页面
3. 使用 `browser_snapshot` 获取页面快照，检查页面是否正常加载
4. 检查导航栏是否正常显示
5. 检查"登录"和"免费注册"按钮是否存在（通过快照查看）

**预期结果**:
- ✅ 页面正常加载，显示"AI项目管理系统"
- ✅ 导航栏正常显示
- ✅ "登录"和"免费注册"按钮存在

**如果失败**: 记录问题到测试结果表格，但不影响后续测试

**记录方式**: 使用Filesystem MCP工具更新桌面上的 `test_results.csv` 文件

---

### 1.3 注册功能测试
**测试步骤**:
1. 使用 `browser_navigate` 导航到注册页面 (`http://localhost:3000/register`)
2. 使用 `browser_snapshot` 检查注册页面是否正常加载
3. 使用 `browser_type` 填写注册表单：
   - 用户名: `testuser20250112`
   - 邮箱: `testuser20250112@example.com`
   - 密码: `Test123456!`
   - 确认密码: `Test123456!`
4. 使用 `browser_click` 勾选"我已阅读并同意用户协议和隐私政策"复选框
5. 使用 `browser_click` 点击"创建账户"按钮
6. 使用 `browser_wait_for` 等待注册结果
7. 使用 `browser_snapshot` 检查注册结果
8. 使用 `browser_network_requests` 查看注册API请求
9. 使用 `browser_console_messages` 查看控制台错误

**预期结果**:
- ✅ 注册页面正常加载
- ✅ 表单验证正常（密码强度显示）
- ✅ 注册成功，显示成功提示
- ✅ 自动跳转到登录页面

**如果失败**: 
- 记录错误信息到测试结果表格
- 记录网络请求状态码和错误信息
- 记录控制台错误信息
- 尝试使用不同的邮箱地址
- **如果注册持续失败，需要先修复注册功能才能继续测试**

**记录方式**: 在测试结果表格中记录状态、耗时、错误信息、备注

---

### 1.4 登录功能测试
**测试步骤**:
1. 使用 `browser_navigate` 导航到登录页面 (`http://localhost:3000/login`)
2. 使用 `browser_snapshot` 检查登录页面是否正常加载
3. **记录开始时间**（用于计算登录耗时）
4. 使用 `browser_type` 填写登录表单：
   - 邮箱: 使用刚注册的邮箱或现有测试账号
   - 密码: 对应的密码
5. 使用 `browser_click` 点击"登录"按钮
6. 使用 `browser_wait_for` 等待登录结果（最多等待5秒）
7. **记录结束时间，计算登录耗时**
8. 使用 `browser_snapshot` 检查登录结果
9. 使用 `browser_network_requests` 查看登录API请求
10. 使用 `browser_console_messages` 查看控制台错误

**预期结果**:
- ✅ 登录页面正常加载
- ✅ 登录成功，显示成功提示
- ✅ **登录时间 < 3秒**（验证上周修复的性能优化）
- ✅ 自动跳转到Dashboard页面 (`/dashboard`)
- ✅ Token已正确生成并存储

**如果失败**: 
- 记录错误信息到测试结果表格
- 记录网络请求状态码和错误信息
- 记录控制台错误信息
- 检查后端日志
- 检查认证配置
- **如果登录持续失败，无法继续后续测试**

**记录方式**: 在测试结果表格中记录状态、耗时（秒）、错误信息、备注

---

## Phase 2: Dashboard功能测试

### 2.1 Dashboard页面加载测试
**测试步骤**:
1. 登录成功后，检查是否自动跳转到Dashboard (`/dashboard`)
2. 检查页面标题是否为"概览"
3. 检查用户信息是否正确显示
4. 检查导航菜单是否正常显示

**预期结果**:
- ✅ 成功跳转到Dashboard页面
- ✅ 页面标题显示"概览"
- ✅ 用户信息正确显示（用户名、邮箱）
- ✅ 导航菜单正常显示（概览、项目管理、文件管理、AI问答、知识笔记、数据分析、团队管理）

**如果失败**: 记录问题，检查DashboardGuard组件

---

### 2.2 Dashboard数据加载测试
**测试步骤**:
1. 检查统计卡片是否正常显示：
   - 我的项目
   - 我的文件
   - AI对话
   - 存储空间
2. 记录数据加载时间
3. 检查"快速操作"区域是否正常显示

**预期结果**:
- ✅ 所有统计卡片正常显示
- ✅ 数据加载时间 < 1秒（验证上周修复的性能优化）
- ✅ "快速操作"区域正常显示（创建项目、上传文件、AI问答、团队协作）

**如果失败**: 记录问题，检查API请求

---

### 2.3 Dashboard导航测试
**测试步骤**:
1. 点击"项目管理"菜单项
2. 检查是否跳转到项目管理页面
3. 返回Dashboard
4. 点击"文件管理"菜单项
5. 检查是否跳转到文件管理页面
6. 返回Dashboard
7. 点击"AI问答"菜单项
8. 检查是否跳转到AI问答页面

**预期结果**:
- ✅ 所有导航链接正常工作
- ✅ 页面跳转正常，无404错误

**如果失败**: 记录问题，检查路由配置

---

## Phase 3: 项目管理功能测试

### 3.1 项目列表页面测试
**测试步骤**:
1. 访问项目管理页面 (`/dashboard/projects`)
2. 检查页面是否正常加载
3. 检查项目列表是否正常显示
4. 检查"新建项目"按钮是否存在

**预期结果**:
- ✅ 页面正常加载
- ✅ 项目列表正常显示（如果有项目）
- ✅ "新建项目"按钮存在且可点击

**如果失败**: 记录问题，检查API请求

---

### 3.2 创建项目测试
**测试步骤**:
1. 点击"新建项目"按钮
2. 填写项目表单：
   - 项目名称: `测试项目_20250112`
   - 项目描述: `这是一个测试项目`
   - 项目阶段: 选择"售前"
3. 点击"创建"按钮
4. 等待创建结果

**预期结果**:
- ✅ 项目创建成功
- ✅ 显示成功提示
- ✅ 项目出现在项目列表中

**如果失败**: 记录问题，检查API请求和数据库

---

### 3.3 项目详情页面测试（验证性能优化）
**测试步骤**:
1. 点击刚创建的项目，进入项目详情页面
2. **记录页面加载时间**（验证上周修复的性能优化）
3. 检查项目信息是否正确显示
4. 检查文件列表是否正常显示
5. 检查项目操作按钮是否正常显示

**预期结果**:
- ✅ 项目详情页面正常加载
- ✅ **页面加载时间 < 500ms**（验证上周修复的性能优化）
- ✅ 项目信息正确显示
- ✅ 文件列表正常显示（如果有文件）
- ✅ 项目操作按钮正常显示（编辑、标记完成、归档、删除）

**如果失败**: 记录问题，检查API请求和性能优化是否生效

---

### 3.4 项目编辑测试
**测试步骤**:
1. 在项目详情页面，点击"编辑"按钮
2. 修改项目名称和描述
3. 点击"保存"按钮
4. 检查项目信息是否更新

**预期结果**:
- ✅ 项目编辑成功
- ✅ 项目信息正确更新

**如果失败**: 记录问题，检查API请求

---

## Phase 4: 文件管理功能测试（验证性能优化）

### 4.1 文件列表页面测试
**测试步骤**:
1. 访问文件管理页面 (`/dashboard/files`)
2. 检查页面是否正常加载
3. **记录页面加载时间**（验证上周修复的性能优化）
4. 检查文件列表是否正常显示
5. 检查"上传文件"按钮是否存在

**预期结果**:
- ✅ 页面正常加载
- ✅ **页面加载时间 < 500ms**（验证上周修复的性能优化）
- ✅ 文件列表正常显示
- ✅ "上传文件"按钮存在且可点击

**如果失败**: 记录问题，检查API请求和性能优化

---

### 4.2 文件上传测试
**测试步骤**:
1. 点击"上传文件"按钮
2. 选择一个测试文件（PDF、DOCX或图片）
3. 选择项目（如果有项目）
4. 点击"上传"按钮
5. 等待上传完成

**预期结果**:
- ✅ 文件上传成功
- ✅ 显示上传进度
- ✅ 文件出现在文件列表中

**如果失败**: 记录问题，检查文件上传API

---

### 4.3 文件查看测试（验证性能优化）
**测试步骤**:
1. 在项目详情页面，点击一个文件
2. **记录文件加载时间**（验证上周修复的性能优化）
3. 检查文件信息是否正确显示
4. 检查文件操作按钮是否正常显示

**预期结果**:
- ✅ 文件信息正确显示
- ✅ **文件加载时间 < 500ms**（验证上周修复的性能优化）
- ✅ 文件操作按钮正常显示（下载、编辑、删除）

**如果失败**: 记录问题，检查API请求和性能优化

---

## Phase 5: Chat功能测试（重点 - 解决无法使用问题）

### 5.1 Chat页面加载测试
**测试步骤**:
1. 访问AI问答页面 (`/dashboard/chat`)
2. 检查页面是否正常加载
3. 检查会话列表是否正常显示
4. 检查"新建对话"按钮是否存在

**预期结果**:
- ✅ 页面正常加载
- ✅ 会话列表正常显示（如果有会话）
- ✅ "新建对话"按钮存在且可点击
- ✅ 没有API错误（之前报告有Failed to fetch错误）

**如果失败**: 
- 记录错误信息
- 检查API请求（`GET /api/v1/chat/conversations`）
- 检查后端日志
- **这是重点问题，需要详细记录**

---

### 5.2 创建新会话测试
**测试步骤**:
1. 点击"新建对话"按钮
2. 填写会话信息：
   - 会话标题: `测试会话_20250112`
   - 选择项目（可选）
3. 点击"创建"按钮
4. 等待创建结果

**预期结果**:
- ✅ 会话创建成功
- ✅ 显示成功提示
- ✅ 会话出现在会话列表中
- ✅ 自动跳转到聊天界面

**如果失败**: 
- 记录错误信息
- 检查API请求（`POST /api/v1/chat/conversations`）
- 检查后端日志

---

### 5.3 发送消息测试
**测试步骤**:
1. 在聊天界面，输入一条测试消息: `你好，请介绍一下这个项目`
2. 点击"发送"按钮
3. 等待AI回复
4. 检查消息是否正常显示
5. 检查AI回复是否正常显示

**预期结果**:
- ✅ 消息发送成功
- ✅ 用户消息正常显示
- ✅ AI回复正常显示（不是错误信息）
- ✅ 消息保存到会话历史中

**如果失败**: 
- 记录错误信息
- 检查API请求（`POST /api/v1/chat/conversations/{id}/messages`）
- 检查AI服务配置
- **这是重点问题，需要详细记录**

---

### 5.4 会话列表加载测试
**测试步骤**:
1. 返回会话列表
2. 检查会话列表是否正常加载
3. 检查会话信息是否正确显示（标题、最后消息、时间）

**预期结果**:
- ✅ 会话列表正常加载
- ✅ 会话信息正确显示
- ✅ 没有API错误

**如果失败**: 
- 记录错误信息
- 检查API请求（`GET /api/v1/chat/conversations`）

---

### 5.5 项目关联Chat测试
**测试步骤**:
1. 在项目详情页面，点击"AI问答"按钮
2. 检查是否自动创建项目关联的会话
3. 检查项目上下文是否正确加载
4. 发送一条关于项目的问题
5. 检查AI回复是否包含项目相关信息

**预期结果**:
- ✅ 自动创建项目关联会话
- ✅ 项目上下文正确加载
- ✅ AI回复包含项目相关信息

**如果失败**: 记录问题，检查项目上下文加载逻辑

---

## Phase 6: 团队管理功能测试（重点 - 解决无法添加问题）

### 6.1 团队管理页面加载测试
**测试步骤**:
1. 访问团队管理页面 (`/dashboard/team`)
2. 检查页面是否正常加载
3. 检查用户列表是否正常显示
4. 检查标签页是否正常显示（用户管理、项目团队）

**预期结果**:
- ✅ 页面正常加载
- ✅ 用户列表正常显示（如果有用户）
- ✅ 标签页正常显示
- ✅ 没有API错误（之前报告有Failed to fetch错误）

**如果失败**: 
- 记录错误信息
- 检查API请求（`GET /api/v1/auth/users`）
- 检查后端日志
- **这是重点问题，需要详细记录**

---

### 6.2 项目团队管理测试
**测试步骤**:
1. 切换到"项目团队"标签页
2. 检查项目列表是否正常显示
3. 选择一个项目
4. 检查项目成员列表是否正常显示
5. 检查"添加成员"按钮是否存在

**预期结果**:
- ✅ 项目列表正常显示
- ✅ 项目成员列表正常显示
- ✅ "添加成员"按钮存在且可点击

**如果失败**: 记录问题，检查API请求

---

### 6.3 添加项目成员测试（重点）
**测试步骤**:
1. 点击"添加成员"按钮
2. 填写成员信息：
   - 邮箱: 使用已注册用户的邮箱（如 `testuser20250112@example.com`）
   - 角色: 选择"成员"
3. 点击"添加"按钮
4. 等待添加结果

**预期结果**:
- ✅ 成员添加成功
- ✅ 显示成功提示
- ✅ 成员出现在成员列表中
- ✅ 成员信息正确显示（用户名、邮箱、角色）

**如果失败**: 
- 记录错误信息
- 检查API请求（`POST /api/v1/projects/{id}/members`）
- 检查后端日志
- 检查用户是否存在
- 检查权限验证
- **这是重点问题，需要详细记录**

---

### 6.4 编辑成员角色测试
**测试步骤**:
1. 在成员列表中，点击一个成员的"编辑"按钮
2. 修改成员角色（如从"成员"改为"管理员"）
3. 点击"保存"按钮
4. 检查成员角色是否更新

**预期结果**:
- ✅ 成员角色更新成功
- ✅ 显示成功提示
- ✅ 成员角色正确更新

**如果失败**: 记录问题，检查API请求

---

### 6.5 移除项目成员测试
**测试步骤**:
1. 在成员列表中，点击一个成员的"移除"按钮
2. 确认移除操作
3. 检查成员是否从列表中移除

**预期结果**:
- ✅ 成员移除成功
- ✅ 显示成功提示
- ✅ 成员从列表中移除

**如果失败**: 记录问题，检查API请求

---

## Phase 7: 性能验证测试

### 7.1 登录性能验证
**测试步骤**:
1. 清除浏览器缓存和Cookie
2. 访问登录页面
3. 填写登录信息
4. **记录登录时间**（从点击登录到跳转到Dashboard的时间）
5. 重复测试3次，取平均值

**预期结果**:
- ✅ **登录时间 < 3秒**（验证上周修复的性能优化）
- ✅ 登录流程顺畅，无卡顿

**如果失败**: 记录实际时间，检查性能优化是否生效

---

### 7.2 Dashboard加载性能验证
**测试步骤**:
1. 登录后，记录Dashboard页面加载时间
2. 检查API请求是否并行执行
3. 检查是否有重复的API请求

**预期结果**:
- ✅ **Dashboard加载时间 < 1秒**（验证上周修复的性能优化）
- ✅ API请求并行执行
- ✅ 没有重复的API请求

**如果失败**: 记录实际时间，检查性能优化是否生效

---

### 7.3 项目详情页面加载性能验证
**测试步骤**:
1. 访问项目详情页面
2. **记录页面加载时间**
3. 检查API请求数量
4. 检查是否有重复的API请求

**预期结果**:
- ✅ **页面加载时间 < 500ms**（验证上周修复的性能优化）
- ✅ API请求数量 <= 2（之前有3次重复请求）
- ✅ 没有重复的API请求

**如果失败**: 记录实际时间和API请求数量，检查性能优化是否生效

---

### 7.4 文件列表加载性能验证
**测试步骤**:
1. 访问文件管理页面
2. **记录页面加载时间**
3. 检查API请求数量
4. 检查文件列表渲染时间

**预期结果**:
- ✅ **页面加载时间 < 500ms**（验证上周修复的性能优化）
- ✅ 文件列表快速渲染

**如果失败**: 记录实际时间，检查性能优化是否生效

---

## 📊 测试检查清单

### 基础功能
- [ ] 环境准备完成
- [ ] 首页正常加载
- [ ] 注册功能正常（如果失败，需要先修复）
- [ ] 登录功能正常（如果失败，无法继续测试）

### Dashboard功能
- [ ] Dashboard页面正常加载
- [ ] 数据统计正常显示
- [ ] 导航菜单正常工作

### 项目管理功能
- [ ] 项目列表正常显示
- [ ] 创建项目功能正常
- [ ] 项目详情页面正常加载（验证性能优化）
- [ ] 项目编辑功能正常

### 文件管理功能
- [ ] 文件列表正常显示（验证性能优化）
- [ ] 文件上传功能正常
- [ ] 文件查看功能正常（验证性能优化）

### Chat功能（重点）
- [ ] Chat页面正常加载
- [ ] 创建会话功能正常
- [ ] 发送消息功能正常
- [ ] 会话列表正常加载
- [ ] 项目关联Chat功能正常

### 团队管理功能（重点）
- [ ] 团队管理页面正常加载
- [ ] 项目团队管理功能正常
- [ ] **添加项目成员功能正常**（重点）
- [ ] 编辑成员角色功能正常
- [ ] 移除项目成员功能正常

### 性能验证
- [ ] 登录性能 < 3秒
- [ ] Dashboard加载性能 < 1秒
- [ ] 项目详情页面加载性能 < 500ms
- [ ] 文件列表加载性能 < 500ms

---

## 🎯 测试执行策略

### 测试顺序
1. **Phase 1** - 必须先完成，否则无法继续
2. **Phase 2** - Dashboard基础功能
3. **Phase 3** - 项目管理功能（为后续测试准备数据）
4. **Phase 4** - 文件管理功能
5. **Phase 5** - Chat功能（重点测试）
6. **Phase 6** - 团队管理功能（重点测试）
7. **Phase 7** - 性能验证

### 测试中断处理
- 如果Phase 1失败，停止测试，先修复问题
- 如果Phase 2-4失败，记录问题，但不影响后续测试
- 如果Phase 5-6失败，**重点记录**，这是本次测试的重点
- Phase 7可以在任何时候进行，用于验证性能优化

### 问题记录
- 所有问题都要详细记录：
  - 问题描述
  - 复现步骤
  - 错误信息
  - 网络请求详情
  - 控制台错误
  - 截图（如果需要）

---

## 📝 测试结果文件

### CSV测试结果表格
- **文件路径**: `C:\Users\<用户名>\Desktop\test_results.csv`（桌面目录）
- **格式**: CSV表格（UTF-8编码，可用Excel打开）
- **内容**: 测试阶段、测试项、状态、耗时、错误信息、备注
- **更新方式**: 使用 **Filesystem MCP 的 `edit_file` 工具** 直接编辑和保存

### 测试结果更新示例

**每个测试项完成后，使用以下方式更新CSV**:

```json
{
  "path": "C:\\Users\\17625\\Desktop\\test_results.csv",
  "edits": [
    {
      "oldText": "Phase 1,环境准备,⬜,,,",
      "newText": "Phase 1,环境准备,✅,0.5,,"
    }
  ]
}
```

### 注意事项
- 使用 `edit_file` 时需要精确匹配要替换的行，包括所有列
- 状态符号: ✅ 通过 | ❌ 失败 | ⚠️ 部分通过 | ⬜ 未测试
- 耗时格式: 保留2位小数，如 `0.50`、`1.23`
- 每个测试项完成后立即更新，不要等到最后统一更新

---

## 🔍 Chrome DevTools MCP工具使用指南

### 常用工具函数

1. **browser_navigate** - 导航到指定URL
   ```python
   browser_navigate(url="http://localhost:3000/login")
   ```

2. **browser_snapshot** - 获取页面快照（用于检查页面元素）
   ```python
   snapshot = browser_snapshot()
   ```

3. **browser_click** - 点击页面元素
   ```python
   browser_click(element="登录按钮", ref="ref-xxx")
   ```

4. **browser_type** - 输入文本
   ```python
   browser_type(element="邮箱输入框", ref="ref-xxx", text="test@example.com")
   ```

5. **browser_wait_for** - 等待元素或时间
   ```python
   browser_wait_for(text="登录成功")  # 等待文本出现
   browser_wait_for(time=3)  # 等待3秒
   ```

6. **browser_console_messages** - 获取控制台消息
   ```python
   messages = browser_console_messages()
   ```

7. **browser_network_requests** - 获取网络请求
   ```python
   requests = browser_network_requests()
   ```

### 测试流程示例

```python
# 1. 导航到页面
browser_navigate(url="http://localhost:3000/login")

# 2. 获取页面快照
snapshot = browser_snapshot()

# 3. 填写表单
browser_type(element="邮箱输入框", ref="ref-xxx", text="test@example.com")
browser_type(element="密码输入框", ref="ref-xxx", text="password123")

# 4. 点击按钮
browser_click(element="登录按钮", ref="ref-xxx")

# 5. 等待结果
browser_wait_for(time=3)

# 6. 检查结果
snapshot = browser_snapshot()
network_requests = browser_network_requests()
console_messages = browser_console_messages()

# 7. 记录结果到表格
# ... 记录测试结果
```

---

**测试计划版本**: v3.0  
**最后更新**: 2025-01-12  
**更新内容**: 
- ✅ 修改为使用Chrome DevTools MCP工具
- ✅ **必须使用外部Chrome浏览器**（不是Cursor内置浏览器）
- ✅ 测试结果CSV文件保存在Desktop目录（`C:\Users\<用户名>\Desktop\test_results.csv`）
- ✅ 使用Filesystem MCP的 `edit_file` 工具直接编辑CSV文件
- ✅ 明确说明Filesystem MCP工具只能访问Desktop和Documents目录

